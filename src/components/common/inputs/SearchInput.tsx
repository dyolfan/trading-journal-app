import sInputs from "./Inputs.module.css";

const SearchInput: React.FunctionComponent = () => {
	return (
		<div className={sInputs.search_container}>
			<div className={sInputs.search_container_text}>Search..</div>
		</div>
	);
};

export default SearchInput;
